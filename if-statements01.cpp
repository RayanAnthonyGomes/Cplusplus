/* [{ If statements L1 }]
1.   greater than (>), Lesser than (<), Greater or equal (>=), Lesser or equal (<=), equal(==), not (!=)
    These are the common operators for L1.
    If 1 am G@y the code will run. Simple.
*/
// Syntax
 /*
    if (---TEST---){
    cout << "Hehe bouy"
    }
    */
// Ex 1:

#include <iostream>
using namespace std;
int main() {
   int a = 36;
   int b = 3;
   int c = 100;
   if (a==20){
    cout << "Arhja Sagols";
   }
   return 0;
}